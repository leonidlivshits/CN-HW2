# Required interface configuration
INTERFACE_IP=192.168.1.87
INTERFACE_MAC=aa:bb:cc:dd:ee:ff

# Gateway IP (preferred key for HW3). ROUTER_IP is kept as fallback alias.
GATEWAY_IP=192.168.1.1

# DNS servers for HW3 experiments
DNS_PROVIDER_IP=8.8.8.8
ROOT_DNS_IP=198.41.0.4

# Optional legacy fields
# ROUTER_IP=192.168.1.1
# ROUTER_MAC=xx:xx:xx:xx:xx:xx